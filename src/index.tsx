import React from 'react';
import { Text, View } from 'react-native';
import { fn } from '@/utils/index';

const App = () => {
  fn();
  return (
    <View>
      <Text>app</Text>
    </View>
  );
};

export default App;
